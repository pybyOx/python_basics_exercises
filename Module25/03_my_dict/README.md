## Свой словарь

Класс `MyDict`, который ведет себя точно так же, как и обычный словарь 
(унаследован от оригинального `dict`), за исключением того, что метод `get` 
по умолчанию возвращает не `None`, а число `0`.
